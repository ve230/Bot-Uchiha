module.exports = {
    name: "roletarussa",
    execute: async (sock, msg) => {
        const jid = msg.key.remoteJid;
        const resultado = Math.random() < 0.1 ? "💥 Boom! Você perdeu!" : "✅ Sobreviveu!";
        await sock.sendMessage(jid, { text: resultado });
    }
};